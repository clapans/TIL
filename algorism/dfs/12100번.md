## 12100번 2480(Easy)

특정 테스트 케이스 통과 못함

리스트 저장관련 문제 있는 듯 

```python
import sys
import pprint

n = int(sys.stdin.readline())

def up():
    for i in range(n):
        res = []
        tmp = 0
        for j in range(n):
            if tmp == 0:
                tmp = arr[j][i]
            else:
                if arr[j][i] == 0:
                    continue
                if tmp == arr[j][i]:
                    tmp += arr[j][i]
                    res.append(tmp)
                    tmp = 0
                else:
                    res.append(tmp)
                    tmp = arr[j][i]
        res.append(tmp)
        while len(res) < n:
            res.append(0)
        for x in range(n):
            arr[x][i] = res[x]

def down():
    for i in range(n):
        res = []
        tmp = 0
        for j in range(n-1,-1,-1):
            if tmp == 0:
                tmp = arr[j][i]
            else:
                if arr[j][i] == 0:
                    continue
                if tmp == arr[j][i]:
                    tmp += arr[j][i]
                    res.append(tmp)
                    tmp = 0
                else:
                    res.append(tmp)
                    tmp = arr[j][i]
        res.append(tmp)
        while len(res) < n:
            res.append(0)
        res = res[::-1]
        for x in range(n):
            arr[x][i] = res[x]
def left():
    for i in range(n):
        res = []
        tmp = 0
        for j in range(n):
            if tmp == 0:
                tmp = arr[i][j]
            else:
                if arr[i][j] == 0:
                    continue
                if tmp == arr[i][j]:
                    tmp += arr[i][j]
                    res.append(tmp)
                    tmp = 0
                else:
                    res.append(tmp)
                    tmp = arr[i][j]
        res.append(tmp)
        while len(res) < n:
            res.append(0)
        for x in range(n):
            arr[i][x] = res[x]


def right():
    for i in range(n):
        res = []
        tmp = 0
        for j in range(n-1,-1,-1):
            if tmp == 0:
                tmp = arr[i][j]
            else:
                if arr[i][j] == 0:
                    continue
                if tmp == arr[i][j]:
                    tmp += arr[i][j]
                    res.append(tmp)
                    tmp = 0
                else:
                    res.append(tmp)
                    tmp = arr[i][j]
        res.append(tmp)
        while len(res) < n:
            res.append(0)
        res = res[::-1]
        for x in range(n):
            arr[i][x] = res[x]

res = 0
def dfs(cnt,arr_,v):
    global res
    if cnt == 5:
        for t in range(n):
            res = max(res,max(arr_[t]))         
    else:
        for i in [1,2]:
            if i == 0:
                v.append('l')
                left()
                dfs(cnt+1,arr_,v)
                arr_ = arr[:]
                v.pop()
            elif i == 1:
                v.append('r')
                right()
                dfs(cnt+1,arr_,v)
                arr_ = arr[:]
                v.pop()
            elif i == 2:
                if v == ['r']:
                    pprint.pprint(arr)
                v.append('u')
                up()
                dfs(cnt+1,arr_,v)
                arr_ = arr[:]
                v.pop()
            else:
                v.append('d')
                down()
                dfs(cnt+1,arr_,v)
                arr_ = arr[:]
                v.pop()

arr = []
for i in range(n):
    arr.append(list(map(int,sys.stdin.readline().split())))


dfs(0,arr[:],[])
print(res)

'''


right()
up()
right()
up()
up()
pprint.pprint(arr)
'''

```

